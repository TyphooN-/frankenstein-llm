# AMD GPU profile snapshot

Copied from `/etc/default/amdgpu-custom-state.card*` following the operator update on 2026-09-09. All three files were verified byte-for-byte against their sources. Relative to `2026-09-08T1924-0400`, card1 changed; card0 and card2 are unchanged.

This is a backup of persisted configuration, not a claim of effective settings or stability qualification. No GPU settings or services were changed. Verify the archive with `sha256sum -c SHA256SUMS`.
